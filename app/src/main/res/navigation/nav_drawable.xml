<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_drawable"
    app:startDestination="@id/drawFragment">

    <fragment
        android:id="@+id/drawFragment"
        android:name="vn.root.app.pages.draws.DrawFragment"
        android:label="fragment_draw"
        tools:layout="@layout/fragment_boarding">
        <action
            android:id="@+id/action_drawFragment_to_faceFragment"
            app:destination="@id/faceFragment" />

    </fragment>

    <fragment
        android:id="@+id/faceFragment"
        android:name="vn.root.app.pages.draws.face.FaceFragment"
        android:label="fragment_face"
        tools:layout="@layout/fragment_face" />

</navigation>